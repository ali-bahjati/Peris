Peris Pattern Recognizer

It's a program that learn a pattern, then can recognize it.

Peris is licensed under GPLv3.

////Installation////
We don't have installation yet, for using you just need to compile it and run the binary.

you will need qt4 and g++.

to enter the directory:
	cd PATH
to compile:
	qmake
	make
then run SimpleDrawer binary file. Simple Drawer is a qt GUI to test the program.
